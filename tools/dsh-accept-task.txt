Call the tool `kvflow_start` exactly once, with exactly these arguments: requirement="add a cube(a) helper to src/calc.py and prove it with the registered test profile", project_id="demo-python-api-163aa7cc", template="feature".

Rules:
- Do not call any other tool.
- Do not inspect the repository yourself.
- After the call, print the job_id and the node ids from the result, and nothing else.
